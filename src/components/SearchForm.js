import styled from 'styled-components';

const SearchForm = styled.form `
    max-width: 400px;
    background: white;
    border-radius: 3px;
    height: 250px;
    margin: 4vw auto;
`;

export default SearchForm;